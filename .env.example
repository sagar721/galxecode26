# Copy to .env and fill in real values. NEVER commit .env.
# --- Runtime ---
NODE_ENV=development
PORT=3000

# --- Database ---
DATABASE_URL=

# --- Auth / sessions (if needed) ---
JWT_SECRET=
SESSION_SECRET=

# --- AI providers (only fill the one you use) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# --- Third-party APIs (add as needed) ---
API_KEY=
