# GALXECODE 26

## Problem
TBD

## Solution
TBD

## Features
TBD

## Tech Stack
TBD

## Architecture
TBD

## Setup
```bash
git clone <repo-url>
cd galxecode26
cp .env.example .env   # fill in values
# frontend: cd frontend && npm install && npm run dev
# backend:  cd backend  && npm install && npm run dev
# ai:       cd ai       && pip install -r requirements.txt
```

## Environment Variables
See `.env.example` for the full list. Never commit `.env`.

## Running Locally
TBD (depends on chosen stack)

## Team
5 members. See `docs/TEAM_WORKFLOW.md` for roles and workflow.

## Development Workflow
See `docs/TEAM_WORKFLOW.md` and `docs/DEVELOPMENT_CHECKLIST.md`.
